@model IEnumerable<TaskManagementSystem.Models.Tasks>

<h2>Task List</h2>

<a href="@Url.Action("Create", "Tasks")" class="btn btn-success mb-3">Create New Task</a>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Description</th>
            <th>Due Date</th>
            <th>Details</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var task in Model)
        {
            <tr>
                <td>@task.Id</td>
                <td>@task.Name</td>
                <td>@task.Description</td>
                <td>@task.DueDate</td>
                <td>
                    <a href="@Url.Action("Details", "Student", new { id = task.Id })">View Details</a>
                </td>
                <td>
                    <a href="@Url.Action("Edit", "Student", new { id = task.Id })">Edit</a>
                </td>
                <td>
                    <a href="@Url.Action("Delete", "Student", new { id = task.Id })">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<a href="@Url.Action("Create", "Tasks")" class="btn btn-primary">Create New Task</a>